# Base URL for the backend API.
# The app will try:
#   GET/POST   {BASE}/notes
#   PUT/DELETE {BASE}/notes/:id
# and also fall back to {BASE}/api/notes paths.
#
# If unset or the backend is unreachable, the app uses localStorage.
REACT_APP_API_BASE=http://localhost:8000
